import streamlit as st

# 1. 페이지 설정
st.set_page_config(page_title="청호방재 필드마스터", layout="wide")

# 2. 사이드바 (메뉴)
st.sidebar.title("🛠️ 관리 메뉴")
menu = st.sidebar.radio("이동할 메뉴", ["현장 대시보드", "점검 기록", "설정"])

# 3. 메인 화면
st.title("🚀 청호방재 필드마스터")
st.info("현재 시스템이 가동 중입니다. 나중에 캔버스나 데이터를 연결할 수 있습니다.")

if menu == "현장 대시보드":
    st.subheader("📊 현장 진행 현황")
    # 샘플 데이터로 일단 모양만 잡기
    col1, col2, col3 = st.columns(3)
    col1.metric("전체 현장", "12개")
    col2.metric("점검 완료", "8개")
    col3.metric("진행 중", "4개")
    
    st.write("---")
    st.warning("💡 데이터 연결 준비 완료! 나중에 엑셀이나 시트를 연결하면 여기에 표가 나타납니다.")

elif menu == "점검 기록":
    st.subheader("✍️ 오늘의 점검 기록")
    st.text_input("현장명을 입력하세요")
    st.date_input("점검 날짜")
    st.button("기록 저장 (연습용)")

else:
    st.subheader("⚙️ 시스템 설정")
    st.write("앱 버전: 1.0.0")
    st.write("관리자: 청호방재")
